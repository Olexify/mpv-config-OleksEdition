# Subfolders of the playing file's folder: ignore = leave them out,
# recursive = include the videos inside them. Toggled from Sort > Include
# Subfolders, which rewrites this line.
directory_mode=ignore
