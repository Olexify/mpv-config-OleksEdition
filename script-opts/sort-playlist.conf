# auto: order applied whenever the playlist changes.
#   explorer  = same column and direction as the Explorer window showing the folder
#   name_asc, name_desc, date_new, date_old, off
auto=explorer
# Used by "explorer" when no Explorer window has the folder open.
fallback=name_asc
